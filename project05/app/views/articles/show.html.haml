%h1
  Articl.es
  
%p
  \.\.\.what is &quot;Articl&quot; in Spanish anyway?

= link_to 'Back', articles_path, :title => 'Back to list of Articles' | 
= link_to 'Edit', edit_article_path(@article), :title => 'Edit Article' | 
= link_to 'Destroy', @article, confirm: 'Are you sure?', method: :delete, :title => 'Delete this article' |
= article_mailto 'E-mail', @article

%h2
  = @article.title

%p
  By = @article.author. = @article.edits edits since = @article.created_at.to_s(:created_day)

= simple_format(@article.body)